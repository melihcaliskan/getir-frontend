import Fade from 'react-reveal/Fade';

const Loader = (props) => {
  return (
    <Fade>
      <div class="loader">
        <img width="320" src="/logo-mobile.png" alt="getir" />
      </div>
    </Fade>
  )
}

export default Loader